@font-face {
    font-family: 'SF UI Display';
    src: local('SF UI Display'), local('SFUIDisplay-Regular'),
         url('https://github.com/Jellyfinovich/Koda/blob/main/fonts/SFUIDisplay-Regular.woff2') format('woff2'),
         url('https://github.com/Jellyfinovich/Koda/blob/main/fonts/SFUIDisplay-Regular.woff') format('woff');
    font-weight: 400; /* Regular */
    font-style: normal;
}

body {
    font-family: 'SF UI Display', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    margin: 0;
    padding: 0;
    background: #1C1C1C; /* Assuming the background is black as per your Figma design */
}


*, *:before, *:after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

.card {
  width: 170px; /* Width from Figma */
  height: 290px; /* Adjusted height as per your comment */
  margin-bottom: 20px; /* Space below each card */
  overflow: hidden;
  position: relative;
  background-color: #1C1C1C; /* Card background if no image */
}

/* Container for the cards might need to have padding and possibly a max-width */
.card-container {
  display: flex;
  flex-wrap: wrap;
  gap: 20px; /* Adjust the space between cards */
  padding: 20px; /* Adjust the padding around the cards */
  justify-content: center; /* Center the cards within the container */
}

.card img {
    width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
}

.cardContent-button, .cardContent-shadow, .itemDetailImage, .cardOverlayButton-hover, .cardOverlayContainer, .cardImageContainer, .cardPadder, .listItemImage, .listItemImageButton, .listItemButton, .headerButton, .paper-icon-button-light, .innerCardFooter, .blurhash-canvas, .actionSheetMenuItem:hover, .dialog {
border-radius: 20px !important;
}

.card .metadata {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 8px;
    background: rgba(0, 0, 0, 0.5); /* Semi-transparent overlay */
    color: white; /* Text color */
    font-size: 14px; /* Adjust as needed */
}

.card .title {
  font-size: 1rem; /* Adjust based on your design */
  color: #fff; /* Assuming the text is white */
}

.card .info .title {
  font-size: 1rem; /* Adjust as needed */
  font-weight: 600;
}

.card .info .year {
  font-size: 0.875rem;
}

.card .subtitle {
  font-size: 0.875rem; /* Adjust based on your design */
  color: #aaa; /* Assuming a lighter color for subtitles */
}

.header {
    background: #1C1C1C; /* Color from Figma */
    color: white;
    padding: 16px; /* Or as per your design */
    font-size: 24px; /* Adjust as needed */
}

.navigation {
    background: #333; /* Color from Figma */
    color: white;
}


.navigation a {
    color: white;
    text-decoration: none;
    padding: 8px 16px;
    display: inline-block; /* Or flex, depending on your layout */
}

button, .button {
    background: #007aff; /* Example blue color */
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 4px;
    cursor: pointer;
}

button:hover, .button:hover {
    opacity: 0.8;
}

/* Section Title */
.section-title {
  color: #ffffff;
  font-size: 1.5rem; /* Adjust to match Figma */
  margin: 20px 0;
}

/* Section Content Wrapper */
.section-content {
  display: flex;
  flex-wrap: wrap;
  gap: 20px; /* Adjust the space between cards */
  /* Add padding or margins as necessary */
}


@media (max-width: 1200px) {
  .card {
    width: 150px;
    height: 270px;
  }
}

@media (max-width: 992px) {
  .section-content {
    justify-content: center;
  }

  .card {
    width: 140px;
    height: 252px;
  }
}

@media (max-width: 768px) {
  .card {
    width: 120px;
    height: 216px;
  }
}
